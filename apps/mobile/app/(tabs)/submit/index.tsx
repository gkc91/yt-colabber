import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { t } from '@/i18n';

export default function SubmitScreen() {
  return <PlaceholderScreen message={t('placeholder.submit')} />;
}
