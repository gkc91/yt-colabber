# Cloudflare Pages (E2). Var olan dosyalar önce servis edilir; buradakiler kalanlar içindir.
# Dinamik rotalar statik dışa aktarımda "[param].html" adıyla üretiliyor, gelen adres ise
# /review/<uuid> oluyor — eşleştirmeyi burada yapıyoruz.
/review/:id      /review/[taskId].html   200
/submit/:id      /submit/[id].html       200
# Geri kalan her şey istemci tarafı yönlendiriciye düşsün (404 yerine uygulama açılsın).
/*               /index.html             200
