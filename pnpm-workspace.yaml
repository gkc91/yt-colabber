packages:
  - apps/*

# Packages whose install scripts may run. @sentry/cli fetches its binary (source-map upload).
allowBuilds:
  '@sentry/cli': true
  esbuild: true
  # landing: renders the Open Graph image from SVG (apps/landing/scripts/og-image.mjs).
  sharp: true
  unrs-resolver: false
